#  跨网段虚实结合仿真沙盒：构建部署与排错白皮书 (终极详尽版)

## 📑 拓扑概览与设计思想

本平台旨在通过底层桥接技术，将运行在 Ubuntu (借助 OVS 与 VMware) 中的**两套对称虚拟网络**，打通至**现实世界中的实体华为交换机**。最终实现虚拟机 PC 跨越实体设备、跨越网段的端到端 OSPF 互通。

- **虚拟环境 A (左区)：** 涵盖 10.x、20.x、30.x、21.x 等网段。出口路由器 R3 (IP: 192.168.153.200) 对接实体交换机端口 2。
- **虚拟环境 B (右区)：** 涵盖 110.x、120.x、130.x、121.x 等网段。出口路由器 R3 (IP: 192.168.160.200) 对接实体交换机端口 3。
- **核心物理枢纽：** 实体华为交换机，作为 OSPF 骨干区域 (Area 0) 的核心，负责统筹左右两区的路由表。

------

## 🏗️ 第一部分：实体物理交换机配置指南

实体交换机是全网的真实物理中枢。企业级二层交换机的物理接口默认无法直接配置 IP，因此我们采用**“接口划入 VLAN + 配置 Vlanif 虚接口”**的标准规范。

### 1. 设备管理凭证清单

在开始配置前，请通过 Web 或 Console/Telnet 登录实体设备：

- **Web 界面登录：** 账号 `admin` / 密码 `12345678.`
- **Telnet 终端登录：** 账号 `adimin1` / 密码 `1q2w3e4r`

### 2. 接口与 IP 落地配置

进入交换机系统视图，分别配置对接两套虚拟环境的物理端口。

**配置对接环境 A（153 网段，使用 2 号端口）：**

代码段

```
<Huawei> system-view
[Huawei] vlan 153
[Huawei-vlan153] quit
[Huawei] interface GigabitEthernet 0/0/2
[Huawei-GigabitEthernet0/0/2] port link-type access
[Huawei-GigabitEthernet0/0/2] port default vlan 153
[Huawei-GigabitEthernet0/0/2] quit
[Huawei] interface Vlanif 153
[Huawei-Vlanif153] ip address 192.168.153.1 255.255.255.0
[Huawei-Vlanif153] quit
```

**配置对接环境 B（160 网段，使用 3 号端口）：**

代码段

```
[Huawei] vlan 160
[Huawei-vlan160] quit
[Huawei] interface GigabitEthernet 0/0/3
[Huawei-GigabitEthernet0/0/3] port link-type access
[Huawei-GigabitEthernet0/0/3] port default vlan 160
[Huawei-GigabitEthernet0/0/3] quit
[Huawei] interface Vlanif 160
[Huawei-Vlanif160] ip address 192.168.160.1 255.255.255.0
[Huawei-Vlanif160] quit
```

> 📌 **原理解析 (Why)：** 为什么要建 VLAN 和 Vlanif？因为实体华为交换机的物理口（如 GE0/0/2）默认工作在二层模式（MAC 层），无法识别 IP 地址。通过将物理口以 `access` 模式强行划入特定 VLAN，再给该 VLAN 的逻辑接口（Vlanif）配置 IP，就能让这个物理网口拥有三层路由能力，从而与虚拟环境的 R3 (192.168.x.200) 处于同一广播域。

### 3. OSPF 骨干区域宣告

使实体交换机参与全网路由计算，学习隐藏在虚拟环境深处的 PC 网段。

代码段

```
[Huawei] ospf 1
[Huawei-ospf-1] area 0.0.0.0
[Huawei-ospf-1-area-0.0.0.0] network 192.168.153.0 0.0.0.255
[Huawei-ospf-1-area-0.0.0.0] network 192.168.160.0 0.0.0.255
[Huawei-ospf-1-area-0.0.0.0] return
<Huawei> save
```

> 📌 **原理解析 (Why)：** `network` 命令的作用是“激活指定网段接口的 OSPF 发送和接收能力”。当 `153.0` 和 `160.0` 被宣告进 Area 0 后，交换机就会从 2 号和 3 号端口疯狂向外发送 OSPF Hello 包。一旦虚拟机的 R3 收到并回应，邻居关系 (`Full`) 建立，全网路由表就会瞬间同步。

------

## 💻 第二部分：虚拟宿主机底层桥接与自动化配置

这是整个工程最容易翻车的阶段。虚实结合的本质，是让虚拟机的流量“借用”宿主机的物理网卡发出去。必须严格剥夺宿主机操作系统对这块网卡的控制权，并精确下发对称的网络配置。

### 1. VMware 桥接绝对锁定 (Windows/Mac 宿主机操作)

1. 打开 VMware Workstation -> **编辑 (Edit)** -> **虚拟网络编辑器 (Virtual Network Editor)**（点击右下角获取管理员权限）。
2. 选中 **VMnet0 (桥接模式)**。
3. 在“已桥接至(Bridged to)”下拉菜单中，**严禁使用“自动 (Automatic)”**。
4. **手动精准选择**你的 USB 拓展坞或真实插着网线的物理网卡名称（如 `Realtek USB GbE Family Controller`）。

### 2. Ubuntu 宿主机的网络净化 (Linux 虚拟机操作)

为防止 Ubuntu 图形界面劫持底层物理包，必须剥夺其对桥接网卡（假设为 `ens37`）的管理权。在 Ubuntu 终端执行：

Bash

```
# 1. 禁用图形网络管理器，防止其自作主张去请求 DHCP
sudo nmcli device set ens37 managed no
# 2. 清洗网卡，剥夺其所有 IP 地址，使其变为纯二层透传管道
sudo ip addr flush dev ens37
# 3. 强制网卡上线，并开启混杂模式 (PROMISC，拦截虚拟 MAC 的关键)
sudo ip link set ens37 up promisc on
```

### 3. 自动化配置 (`auto_config.py`) 防坑机制

在通过 Python 脚本下发配置时，必须注意时机与系统底层进程的冲突：

- **时机控制**：点击“拉起底层网络”后，**强制等待至少 2 分钟**。虚拟路由器解压固件和启动需要时间，过早连入 Telnet 会遭遇黑屏，导致脚本崩溃。

### 4. 自动化脚本 IP 与 OSPF 路由映射规范 (核心示例)

整个虚拟拓扑分为 **环境A (左区)** 和 **环境B (右区)**，呈现完美的镜像对称。在 `auto_config.py` 中，必须严格按照以下规划下发 IP，并使用 OSPF 将其与物理世界缝合。

**【拓扑与 IP 规划对照表】**

| **节点类型**      | **环境 A (左区)**      | **环境 B (右区)**      | **原理解析**                                |
| ----------------- | ---------------------- | ---------------------- | ------------------------------------------- |
| **PC1 网段**      | 10.1.1.2/24            | 110.1.1.2/24           | 终端测试节点，网关分别指向各自的 R1         |
| **PC2 网段**      | 30.1.1.2/24            | 130.1.1.2/24           | 终端测试节点，网关分别指向各自的 R2         |
| **R1-R3 互联**    | 20.1.1.x/24            | 120.1.1.x/24           | 内部骨干路由网段                            |
| **R2-R3 互联**    | 21.1.1.x/24            | 121.1.1.x/24           | 内部骨干路由网段                            |
| **R3 对外桥接口** | **192.168.153.200/24** | **192.168.160.200/24** | **关键！此接口通过 OVS 直接挂载到物理网卡** |

**【代码映射示例：以环境 B (右区) 为例】**

在 `auto_config.py` 中，R3 作为虚实结合的边界路由器，其配置代码最为关键。它不仅要连接内部的 120 和 121 网段，还要将出海口的 `160.200` IP 宣告进 OSPF Area 0，以便与物理交换机对接。

```python
    # ---------------------------------------------------------
    # 边界路由器 R3 (环境 B) 的核心配置示例
    # ---------------------------------------------------------
    r3_cmds = [
        "system-view", 
        # 1. 配置面向内部 R1 的接口
        "interface Ethernet1/0/0", 
        "ip address 120.1.1.1 24", 
        "commit", "quit",
        
        # 2. 配置面向内部 R2 的接口
        "interface Ethernet1/0/1", 
        "ip address 121.1.1.1 24", 
        "commit", "quit",
        
        # 3. 核心：配置面向实体交换机的桥接接口
        "interface Ethernet1/0/2", 
        "ip address 192.168.160.200 24", # 必须与实体交换机的 3 号口在同一网段
        "commit", "quit",
        
        # 4. OSPF 宣告：将内外网络彻底缝合
        "ospf 1", 
        "area 0", 
        "network 120.1.1.0 0.0.0.255",     # 宣告内部骨干网
        "network 192.168.160.0 0.0.0.255", # 关键：宣告物理桥接网段，与物理交换机建邻居
        "quit",
        
        "area 1", 
        "network 121.1.1.0 0.0.0.255",     # 宣告另一条内部骨干网
        "commit", "return"
    ]
```

------

## 🤖 第三部分：自动化脚本防坑下发

在通过 Python `telnetlib` 下发网络配置时，由于虚拟机系统底层的特性，必须加入特定的防抖与净化机制。

### 1. 时机控制机制

- 点击“拉起底层网络”后，**强制等待至少 3 分钟**再点击“自动化配置”。
- **原因**：虚拟网络设备（尤其是华为/H3C镜像）在后台需要时间解压固件并启动。过早连入 Telnet 会遭遇黑屏或引导日志刷屏，导致 Python 脚本判定符（如 `<` 或 `$`）错位而崩溃退出。

> 

------

## 🚑 附录：终极排错“照妖镜” (Tcpdump 故障隔离法)

当发现“PC 无法跨越网段 Ping 通对面”时，**不要去猜路由，直接在 Ubuntu 宿主机监听物理网卡。** 这能一秒钟划分是“物理界”的锅，还是“虚拟界”的锅。

在 Ubuntu 终端敲入：

Bash

```
sudo tcpdump -i ens37 -n "icmp or arp or ospf"
```

**根据输出日志对号入座：**

- **👻 症状 A：屏幕死寂，没有任何输出**
  - **诊断**：虚拟机 R3 的包根本没出来。
  - **排查**：检查 Ubuntu 的 `ens37` 是否带有 IP 拦截了流量（执行 `ip a` 查看）；检查 OVS 网桥是否成功绑定了 `ens37`（执行 `ovs-vsctl show` 查看）。
- **👻 症状 B：只有 Request，没有 Reply (有去无回)**
  - *例：`ARP, Request who-has 192.168.160.1 tell 192.168.160.200`*
  - **诊断**：虚拟包已经冲出了电脑，但物理世界没理你。
  - **排查**：检查网线是否插紧；检查实体交换机的 2号/3号 端口是否划入了正确的 VLAN 并配置了 IP；检查 VMware 的桥接网卡是不是“自动”跑偏了。
- **👻 症状 C：满屏 `192.168.1.x` 陌生网段数据**
  - **诊断**：数据发去了错误的平行宇宙（你家/公司的局域网）。
  - **排查**：百分之百是 VMware 虚拟网络编辑器里桥接错了物理网卡（桥接到了 Wi-Fi 等其他激活网卡）。
- **👻 症状 D：有来有回，但依然 Ping 不通两端的 PC**
  - **诊断**：物理链路完全正常，纯粹是路由表问题。
  - **排查**：登录实体交换机敲 `display ospf peer brief` 确认 OSPF 邻居是否为 `Full`。如果 Full，检查虚拟路由器 R1/R2/R3 是否有遗漏宣告的深层网段（如 `10.1.1.0` 或 `110.1.1.0`）。